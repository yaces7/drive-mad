{
  "name": "drive-mad-web",
  "version": "1.0.0",
  "description": "Drive Mad style physics car game",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.js --bundle --outfile=public/bundle.js --servedir=public --watch",
    "build": "esbuild src/main.js --bundle --outfile=public/bundle.js --minify",
    "preview": "npx serve public"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "dependencies": {
    "matter-js": "^0.19.0"
  }
}
